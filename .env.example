# OpenRouter API key — https://openrouter.ai/keys
# Copy this file to `.env` for local dev; restart `npm run dev` after edits.
#
# Hosted GitHub Pages: keys are NOT in the shipped JS bundle (push protection).
# Paste your key once in the app menu "OpenRouter key" — it stays in browser localStorage only.

VITE_OPENROUTER_API_KEY=
